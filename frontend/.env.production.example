# Next.js Environment Configuration for Production

# =============================================================================
# API CONFIGURATION
# =============================================================================

# Backend API URL - CRITICAL: Update this with your production API URL
# This should match your server configuration
NEXT_PUBLIC_API_URL=https://app.malikli.store/api/v1

# Alternative if using API subdomain:
# NEXT_PUBLIC_API_URL=https://api.malikli.store/api/v1

# =============================================================================
# OPTIONAL: THIRD-PARTY SERVICES
# =============================================================================

# Payment Gateway (if using Stripe)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_your_stripe_publishable_key

# Google Analytics (if using)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Other public environment variables as needed
# NEXT_PUBLIC_APP_NAME=Malikli Store
# NEXT_PUBLIC_APP_VERSION=1.0.0

# =============================================================================
# INSTRUCTIONS
# =============================================================================

# 1. Copy this file to .env.local in your frontend directory
# 2. Update NEXT_PUBLIC_API_URL with your actual production API URL
# 3. Add any other public environment variables your app needs
# 4. Make sure the API URL is accessible from browsers (CORS configured)
# 5. Test the connection between frontend and backend

# =============================================================================
# IMPORTANT NOTES
# =============================================================================

# - All variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# - Never put sensitive data in NEXT_PUBLIC_ variables
# - The API URL should use HTTPS in production
# - Make sure CORS is properly configured on your Django backend
# - Test API connectivity after deployment

# =============================================================================
# TESTING YOUR CONFIGURATION
# =============================================================================

# Test API connectivity:
# curl https://app.malikli.store/api/v1/
# Should return API information or available endpoints

# Test from browser console:
# fetch('https://app.malikli.store/api/v1/')
#   .then(response => response.json())
#   .then(data => console.log(data));
